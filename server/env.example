# Environment Configuration
NODE_ENV=development
PORT=5000
MONGO_URI=mongodb://localhost:27017/cloudnotes
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
CLIENT_ORIGIN=http://localhost:5173

# SMTP Configuration (optional - if not set, OTP is returned in dev responses)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="CloudNotes <no-reply@cloudnotes.app>"

# Google OAuth Configuration (optional)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Sample Mode (for development without database)
SAMPLE_MODE=false
